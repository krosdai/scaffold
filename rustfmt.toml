# rustfmt configuration shared by every workspace crate.
# rustfmt reads `edition` from here whether it is run directly (as lint-staged
# does on staged .rs files) or via `cargo fmt`, so the edition is not passed on
# the command line or duplicated per crate.
edition = "2024"
style_edition = "2024"
